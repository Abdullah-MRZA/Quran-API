# import arabic_reshaper
from rich import print

# from bidi.algorithm import get_display
from PIL import Image, ImageDraw, ImageFont, ImageFilter
import json


# configuration
# font_size = 36
# width = 200
# height = 100
# back_ground_color = (255, 255, 255)
# font_size = 36
# font_color = (0, 0, 0)
#
# unicode_text = "بِسم اللَه"
# # unicode_text_reshaped = arabic_reshaper.reshape(unicode_text)
# # unicode_text_reshaped_RTL = get_display(unicode_text_reshaped, base_dir="R")
#
# im = Image.new("RGB", (width, height), back_ground_color)
# draw = ImageDraw.Draw(im)
# unicode_font = ImageFont.truetype("Al Majeed Quranic Font_shiped.ttf", font_size)
# draw.text((10, 10), unicode_text, font=unicode_font, fill=font_color)
#
# im.save("text.png")
